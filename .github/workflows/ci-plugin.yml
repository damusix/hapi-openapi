name: ci

on:
    push:
        branches:
            - main
    pull_request:
    workflow_dispatch:

jobs:
    test:
        uses: hapijs/.github/.github/workflows/ci-plugin.yml@min-node-22-hapi-21
